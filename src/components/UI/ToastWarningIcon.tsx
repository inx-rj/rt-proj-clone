import React from "react";
import { Images } from "../../helper/images";

const ToastWarningIcon: React.FC = () => {
  return (
    <img src={Images.ToastWarningIcon} alt="Warning" className=" h-full w-full" />
  );
};

export default ToastWarningIcon;
